<% var userLevel = getUserLevel(experience) %>

<div class="row" style="margin-top:1em;">

    <div class="col s12 valign-wrapper">
        <img src="{{APP_SERVICE_PATH}}/images/<%= userLevel.img %>" class="gem-big">
        <div style="height: 38px; display: inline-block; width: calc(100% - 60px); margin-left: 5px;">
		<span>
			<b>
				<span class="chip tiny clear" style="font-size: 1rem">
					<i class="fa fa-bolt"></i>&nbsp;<%= experience %>&nbsp;<%= userLevel.name %>
				</span>
			</b>
		</span>
            <div class="progress" style="margin: 2px 0; background-color: lightgray;">
                <div class="determinate"
                     style="width: <%= userLevel.percent %>%; background-color: <%= userLevel.color %>"></div>
            </div>
        </div>
    </div>

    <div class="col s12">
        <p>Mientras más utilices la app, <a href="#!" onclick="apretaste.send({command:'PERFIL EXPERIENCIA'})">mayor
                experiencia ganarás</a>, y al subir de nivel podrás disfrutar de mayores beneficios.</p>
        <ul class="collection gems-list">
            <% levels.forEach(function (level) { %>
                <li class="collection-item avatar">
                    <img src="{{APP_SERVICE_PATH}}/images/<%= level.img %>" class="circle">
                    <span style="color: <%= level.titleColor %>"><%= level.name %> </span>
                    <span>(&nbsp;<i class="fa fa-bolt"></i>&nbsp;<%= level.experience %> o más):</span>
                    <% level.benefits.forEach(function (benefit) { %>
                        <br><span>• <%= benefit %>.</span>
                    <% }) %>
                </li>
            <% }) %>
        </ul>
    </div>

    <div class="col s12 center-align">
        <a href="#!" onclick="apretaste.send({command:'PERFIL EXPERIENCIA'})">¿Cómo gano experiencia?</a>
    </div>

    <div class="fixed-action-btn">
        <a class="btn-floating btn-large waves-effect waves-light green" onclick="apretaste.back()">
            <i class="material-icons">arrow_back</i>
        </a>
    </div>
</div>
