<div class="row">
	<div class="col s12 center-align">
		<h1><%= header %></h1>
		<i class="large material-icons"><%= icon %></i>
		<p style="margin:30px 0px;"><%= text %></p>
		<% if (button.back) { %>
			<a class="btn green" onclick="apretaste.back();">Atr&aacute;s</a>
		<% } else { %>
			<a class="btn green" onclick="apretaste.send({command: '<%= button.command %>', data:<%= JSON.stringify(button.data) %>}"><%= button.caption %></a>
		<% }%>
	</div>
</div>