<div class="row green apretaste top-bar">
    <div class="col s12">
        <ul class="tabs tabs-fixed-width green apretaste">
            <li class="tab col s4"><a href="#" class="white-text"
                                      onclick="apretaste.send({command: 'PERFIL', data: {'id': image.id_person}})">Perfil</a>
            </li>
            <li class="tab col s4"><a href="#" class="white-text" onclick="apretaste.send({command: 'PERFIL NIVELES'})">Niveles</a>
            <li class="tab col s4"><a href="#" class="active white-text"
                                      onclick="apretaste.send({command: 'PERFIL IMAGENES', data: {'id': image.id_person}})" )>Imagenes</a></li>
        </ul>
    </div>
</div>

<div class=" row">
    <div class="col s12" style="position: relative">
        <img src="{{APP_IMAGE_PATH}}/<%= image.file %>" class="responsive-img" style="border-radius: 8px">
        <% if(ownProfile) { %>
            <div class="deleteButton modal-trigger" href="#deleteModal">
                <i class="material-icons">close</i>
            </div>
            <% if(image.default != 1){ %>
                <a onclick="selectDefaultImage()">Seleccionar como imagen principal</a>
            <% } %>
        <% } %>
    </div>
</div>

<div id="deleteModal" class="modal">
    <div class="modal-content">
        <p>¿Está seguro de eliminar esta imagen?</p>
    </div>
    <div class="modal-footer">
        <a class="modal-close waves-effect waves-green btn-flat">Cancelar</a>
        <a onclick="deleteImage()" class="modal-close waves-effect waves-red red-text btn-flat">Eliminar</a>
    </div>
</div>
